{
  "name": "fullstack-todo-101",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "gen:api-types": "openapi-typescript backend/openapi.yml -o frontend/src/api-types.ts",
    "dev": "npm run gen:api-types && (npm -w frontend run dev & npm -w backend run start:dev)",
    "dev:fe": "npm run gen:api-types && npm -w frontend run dev",
    "dev:be": "npm -w backend run start:dev",
    "build": "npm run gen:api-types && npm -ws run build",
    "test": "npm -ws run test"
  },
  "devDependencies": {
    "openapi-typescript": "^7.10.1",
    "vite-tsconfig-paths": "^6.0.3"
  }
}
